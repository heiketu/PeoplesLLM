# PEOPLESLLM-PARAMS.md —— 已退役

> **本文档已由 [PARAMETERS.md](PARAMETERS.md) 取代**（2026-08-23，硬化 H0 slice）。
> 全部环境变量 / CLI 参数的语义、默认值与生产配方以 PARAMETERS.md 为准；上手指南见
> [QUICKSTART.md](QUICKSTART.md)。原 358 行内容含过时默认值（如 `GGML_KV_THP=1` 旧语法），
> 为避免误导已删除，历史版本见 git 历史（删除前最后一个版本：`git log -- docs/PEOPLESLLM-PARAMS.md`）；
> 逐项实测数据的权威留档在工作区 `HANDOVER.md`（本地保密，不进仓库）。
